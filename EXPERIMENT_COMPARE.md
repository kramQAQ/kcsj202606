# YOLOv8n 初始模型与 CBAM+BiFPN 改进模型对比分析

## 1. 数据集说明

本次实验使用项目中的 `2_1_dataset` 数据集。

| 项目 | 数值 |
| --- | ---: |
| 数据集来源 | `2_1_dataset` |
| 训练集比例 | 70% |
| 验证集比例 | 30% |
| 类别数量 | 15 |
| 验证集图片数量 | 30 |
| 验证集目标实例数 | 149 |

训练集和验证集由 `split_data.py` 自动划分，划分结果如下：

```text
train set size: 70
val set size: 30
```

## 2. 对比模型

本实验主要对比两个模型：

| 模型 | 说明 | 权重文件 |
| --- | --- | --- |
| YOLOv8n 初始模型 | 基于 Ultralytics 官方 YOLOv8n 框架训练得到的基准模型 | `runs/detect/train_cpu_20260612_112842/weights/best.pt` |
| CBAM+BiFPN 改进模型 | 在 YOLOv8n 基础上加入 CBAM 注意力机制和 BiFPN 加权特征融合 | `runs/detect/train_cbam_bifpn_gpu_20260620_025139/weights/best.pt` |

改进模型配置文件：

```text
yolov8n-cbam-bifpn.yaml
```

## 3. 预训练权重继承情况

为了避免模型从零开始训练导致收敛不稳定，最终改进模型保留了 YOLOv8n 的主干结构、检测头和大部分层级编号，使其能够有效继承 `yolov8n.pt` 的预训练权重。

训练日志显示：

```text
Transferred 383/383 items from pretrained weights
```

这说明 CBAM+BiFPN 改进模型已经成功继承原始 YOLOv8n 的可匹配预训练权重。

前一次大幅改动版本加入了 Ghost、Decoupled Head、Focal Loss 等多个模块，预训练权重继承情况为：

```text
Transferred 162/386 items from pretrained weights
```

该版本由于结构变化过大，预训练权重继承不足，最终精度明显下降。因此最终采用更加稳妥的 CBAM+BiFPN 方案。

## 4. 算法结构对比

### 4.1 YOLOv8n 初始模型

初始 YOLOv8n 主要由以下部分组成：

| 结构 | 作用 |
| --- | --- |
| Backbone | 使用 `Conv` 和 `C2f` 提取多尺度图像特征 |
| Neck | 使用 FPN/PAN 结构进行多尺度特征融合 |
| Head | 使用 YOLOv8 原始 `Detect` 检测头完成分类和边框回归 |
| Loss | 使用 YOLOv8 默认检测损失，包括边框损失、分类损失和 DFL 损失 |

初始模型结构较轻，训练稳定，预训练权重继承充分，是本项目的基准模型。

### 4.2 CBAM+BiFPN 改进模型

改进模型在尽量保持 YOLOv8n 原有结构稳定性的基础上，引入两个实质性改进：

| 改进点 | 实现方式 | 目的 |
| --- | --- | --- |
| CBAM 注意力机制 | 使用 `C2fCBAM` 替换原始 `C2f` | 增强模型对饮片纹理、边缘、轮廓区域的关注 |
| BiFPN 加权特征融合 | 使用 `BiFPN_Concat2` 替换原始 `Concat` | 让不同尺度特征在融合时拥有可学习权重，提高多尺度目标检测能力 |

为了保证训练稳定性，本次最终改进版保留了：

```text
Conv
SPPF
Detect
BCE 分类损失
YOLOv8n 主体层级结构
```

这样做的好处是可以最大限度继承 `yolov8n.pt` 预训练权重，避免由于结构变化过大导致模型重新学习困难。

## 5. 模型复杂度对比

| 模型 | 参数量 Params | 计算量 GFLOPs | best.pt 大小 |
| --- | ---: | ---: | ---: |
| YOLOv8n 初始模型 | 3,008,573 | 8.1 | 6,253,347 bytes |
| CBAM+BiFPN 改进模型 | 3,199,861 fused / 3,205,061 train | 8.3 | 6,657,485 bytes |

从模型复杂度来看，CBAM+BiFPN 改进模型相比初始 YOLOv8n：

| 指标 | 变化 |
| --- | ---: |
| 参数量 | 增加约 6.4% |
| GFLOPs | 增加约 2.5% |
| 模型文件大小 | 增加约 6.5% |

分析：

CBAM 模块引入了通道注意力和空间注意力，因此会增加少量参数。BiFPN 使用可学习权重进行特征融合，参数增加很少。整体来看，改进模型的复杂度只略高于 YOLOv8n，仍然属于轻量级检测模型。

## 6. 验证集指标对比

两个模型均在同一验证集上测试，验证配置如下：

```text
imgsz=640
batch=4
device=0
GPU=NVIDIA GeForce RTX 4050 Laptop GPU
```

整体指标如下：

| 模型 | Precision | Recall | mAP50 | mAP50-95 | 推理时间 |
| --- | ---: | ---: | ---: | ---: | ---: |
| YOLOv8n 初始模型 | 0.97905 | 0.94257 | 0.98786 | 0.80993 | 10.6 ms/img |
| CBAM+BiFPN 改进模型 | 0.95735 | 0.95253 | 0.98018 | 0.80977 | 8.7 ms/img |

## 7. 指标详细分析

### 7.1 Precision 对比

Precision 表示模型预测为正样本的目标中，有多少是真正正确的。

| 模型 | Precision |
| --- | ---: |
| YOLOv8n 初始模型 | 0.97905 |
| CBAM+BiFPN 改进模型 | 0.95735 |

改进模型 Precision 略低，说明加入 CBAM+BiFPN 后，模型检测到的目标更多，但其中部分预测框的误检比例略有增加。

这与注意力机制和多尺度融合的特性有关：CBAM 和 BiFPN 会增强更多潜在目标区域的响应，使模型更积极地检测目标，因此可能带来少量误检。

### 7.2 Recall 对比

Recall 表示真实目标中，有多少被模型成功检测出来。

| 模型 | Recall |
| --- | ---: |
| YOLOv8n 初始模型 | 0.94257 |
| CBAM+BiFPN 改进模型 | 0.95253 |

改进模型 Recall 从 `0.94257` 提升到 `0.95253`，说明 CBAM+BiFPN 对减少漏检有一定帮助。

原因分析：

1. CBAM 能增强饮片纹理和轮廓特征，使模型更容易关注目标区域。
2. BiFPN 通过可学习权重融合不同尺度特征，对尺寸差异较大的饮片目标更友好。
3. 中药饮片存在重叠、遮挡、形态相似等问题，多尺度融合增强后有利于找回部分原模型漏检的目标。

### 7.3 mAP50 对比

mAP50 表示 IoU 阈值为 0.5 时的平均精度。

| 模型 | mAP50 |
| --- | ---: |
| YOLOv8n 初始模型 | 0.98786 |
| CBAM+BiFPN 改进模型 | 0.98018 |

改进模型 mAP50 略低，但仍然保持在较高水平。说明在较宽松的 IoU 判断条件下，两个模型整体检测能力都比较强，初始 YOLOv8n 的准确性略占优势。

### 7.4 mAP50-95 对比

mAP50-95 是更严格的综合指标，会在 IoU 从 0.5 到 0.95 的多个阈值下计算平均精度。

| 模型 | mAP50-95 |
| --- | ---: |
| YOLOv8n 初始模型 | 0.80993 |
| CBAM+BiFPN 改进模型 | 0.80977 |

两者几乎持平，说明改进模型虽然 Precision 和 mAP50 略有下降，但在更严格的综合定位精度上基本保持了 YOLOv8n 的水平。

## 8. 每类目标表现分析

从验证输出可以观察到，CBAM+BiFPN 对部分类别有明显正向作用。

例如：

| 类别 | 现象 |
| --- | --- |
| `gaoliangjiang` | CBAM+BiFPN 的 mAP50-95 达到 `0.928`，高于初始模型的 `0.891` |
| `baishao` | CBAM+BiFPN 的 mAP50-95 达到 `0.820`，高于初始模型的 `0.741` |
| `baixianpi` | CBAM+BiFPN 的召回率明显提升，达到接近 `0.995` |
| `sangzhi` | CBAM+BiFPN 的 mAP50-95 达到 `0.851`，高于初始模型的 `0.782` |

这些类别可能具有纹理细节明显、边缘形状较复杂或易受背景干扰的特点，因此注意力机制和多尺度融合能带来更好的特征表达。

但也有部分类别表现下降，例如：

| 类别 | 现象 |
| --- | --- |
| `dazao` | mAP50 和 mAP50-95 均低于初始模型 |
| `yuzhu` | mAP50-95 低于初始模型 |
| `gancao` | mAP50-95 低于初始模型 |

这说明当前改进并不是对所有类别都稳定增益。由于数据集规模较小，不同类别的样本数量有限，加入注意力和加权融合后可能会使模型对某些类别的特征偏向发生变化。

## 9. 推理速度分析

| 模型 | 推理时间 |
| --- | ---: |
| YOLOv8n 初始模型 | 10.6 ms/img |
| CBAM+BiFPN 改进模型 | 8.7 ms/img |

本次验证中 CBAM+BiFPN 的推理时间略低。不过由于验证集较小，单次测试的速度会受到 GPU 状态、缓存、后处理耗时等因素影响，因此推理速度只能作为参考。

从结构上看，CBAM+BiFPN 的参数量和 GFLOPs 略高，理论上并不一定比 YOLOv8n 更快。更可靠的速度对比应通过多次重复测试取平均值。

## 10. 综合结论

与 YOLOv8n 初始模型相比，CBAM+BiFPN 改进模型具有以下特点：

优点：

1. 成功继承 `yolov8n.pt` 预训练权重，训练稳定性明显优于大幅改动版本。
2. Recall 从 `0.94257` 提升到 `0.95253`，漏检情况有所减少。
3. mAP50-95 与 YOLOv8n 基本持平，说明整体定位能力没有明显下降。
4. 对部分纹理明显、形态复杂的饮片类别有更好的检测表现。

不足：

1. Precision 从 `0.97905` 降至 `0.95735`，误检略有增加。
2. mAP50 从 `0.98786` 降至 `0.98018`，宽松 IoU 条件下的平均精度略低。
3. 参数量和模型大小略有增加。
4. 数据集规模较小，改进效果在不同类别之间存在波动。

最终结论：

CBAM+BiFPN 改进模型属于有效的结构性改进。它在保持 YOLOv8n 整体检测能力的基础上，提高了召回率，增强了对复杂纹理和多尺度饮片目标的检测能力。虽然它没有在所有指标上全面超过 YOLOv8n，但相比大幅修改网络结构的版本，该方案更加稳定，也更适合作为本项目的最终改进模型。
